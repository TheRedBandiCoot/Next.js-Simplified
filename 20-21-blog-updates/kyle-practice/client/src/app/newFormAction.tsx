// 'use client';
// import { useActionState } from 'react';
export default function NewFormAction() {
  // throw new Error(
  //   'Install Latest react and next `pnpm up next@rc react@rc react-dom@rc eslint-config-next@rc` '
  // );
  // const [state, submit] = useActionState(ps => (ps += 1), 0);
  return (
    <></>
    // <form action={submit}>
    // <h1>count : {state}</h1>
    // <button>incr</button>
    // </form>
  );
}
