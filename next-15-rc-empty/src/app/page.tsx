/**
 * //@ code for install `pnpm dlx create-next-app@rc --turbo --empty --use-pnpm .`
 * //@ `--turbo` => For use turbo
 * //@ `--empty` => For create new project with minimal setup
 */

export default function Home() {
  return (
    <main>
      <div>Hello world!</div>
    </main>
  );
}
